#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main() {
    ll n, x, y, mod = 1e9 + 7;
    scanf("%lld %lld", &n, &x);
    vector<ll> coins(n);
    for (int i = 0; i < n; i++) {
        cin >> y;
        coins[i] = y;
    }
    
    return 0;
}