#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main() {
    ll n, m, x;
    cin >> n >> m;
    vector<ll>nums(n);
    for (int i = 0; i < n; i++) {
        cin >> x;
        nums[i] = x;
    }

    return 0;
}